// declare class

// export class using module.exports
class Shape {
  constructor(color) {
    this.color = color;
  }

  drawShape() {
    // implementation for drawing the shape
  }

  calculateArea() {
    // implementation for calculating the area of the shape
  }
}

module.exports = Shape;
